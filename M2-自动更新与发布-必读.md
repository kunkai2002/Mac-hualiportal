# 桌面版 M2 — 发布与自动更新（你来执行的步骤）

本次（v0.2.0）桌面壳改动：自绘标题栏（decorations:false，网页画顶栏）、窗口控制/开浏览器/任务栏角标/新窗口开应用都走本地桥（127.0.0.1:3710）、Tauri 自动更新。
代码我已改完并提交到本地 `main`。**自动更新需要你做 3 件事**（签名密钥只能你保管，我不能碰），然后推送即可。

---

## 一、生成更新签名密钥（一次性，只在你本机做）

在 `huali-desktop` 目录运行（PowerShell）：

```powershell
npm install
npm run tauri signer generate -- -w "$HOME\.tauri\huali-portal-updater.key"
```

- 会让你设一个密码（可留空直接回车；设了就记住）。
- 生成两个东西：
  - **私钥**：文件 `~\.tauri\huali-portal-updater.key`（**绝密，别提交、别外发**）。
  - **公钥**：终端会打印一段 `Public key: ...`，也写在 `huali-portal-updater.key.pub` 里（**不机密**）。

## 二、把公钥贴进配置

打开 `src-tauri/tauri.conf.json`，把：

```json
"pubkey": "PASTE_YOUR_TAURI_PUBKEY_HERE"
```

里的占位串替换成上一步的**公钥全文**（一行）。保存。

## 三、在 GitHub 仓库设两个 Secret

仓库 `kunkai2002/Mac-hualiportal` → Settings → Secrets and variables → Actions → New repository secret：

| 名称 | 值 |
|---|---|
| `TAURI_SIGNING_PRIVATE_KEY` | 私钥文件 `huali-portal-updater.key` 的**全文内容**（用记事本打开整段复制） |
| `TAURI_SIGNING_PRIVATE_KEY_PASSWORD` | 上面设的密码；没设就建一个值为空的 secret |

---

## 四、推送 + 打标签出包（每次发新版都这样）

```powershell
cd D:\kun-workspace\huali-desktop
git push origin main
git tag v0.2.0
git push origin v0.2.0
```

GitHub Actions（Build Desktop）会自动：编 Mac(.dmg)+Win(.exe)、签名更新包、生成 `latest.json`、发布到 Release。
- 下载按钮用的固定名 `HualiPortal-setup.exe` / `HualiPortal-universal.dmg` 照旧能用。
- 自动更新清单 `latest.json` 也在该 Release 里（updater 端点已配 `releases/latest/download/latest.json`）。

## 五、自动更新怎么生效

- 这次 v0.2.0 是「带自动更新能力的第一版」——**你和同事要先装一次这个 v0.2.0**（从 Release 下载安装）。
- 之后你只要再发 v0.2.1、v0.2.2…（改完代码→`git tag v0.2.1`→push tag），**已装 v0.2.0 的人开软件后，顶栏会出现「有新版 vX」按钮，点一下自动下载安装并重启**，不用再手动重装。
- 版本号要递增：改 `src-tauri/tauri.conf.json` 的 `version` 和 `src-tauri/Cargo.toml` 的 `version`（两处保持一致），再打对应的 `v` 标签。

---

## 注意 / 排错

- **公钥忘了贴**：Release 出来了但 updater 永远验签失败、更新不动 → 回到第二步贴公钥重发。
- **私钥/密码对不上 secret**：Actions build 会在签名步骤报错 → 核对第三步两个 secret。
- **第一次没有「旧版本」可更新**：自动更新是 v0.2.0→之后版本之间生效；v0.1.0 的旧壳不会自动升到 v0.2.0（需手动装一次 v0.2.0）。
- **Mac 未签名**（无 Apple 开发者证书）：首次打开要右键→打开。这与更新签名是两回事（更新签名是 Tauri 自己的，不是 Apple 公证）。
- 网页端（workers.dev）已同步部署 v0.19.42：装了新壳就会显示自绘顶栏；旧壳/浏览器不受影响（仍是系统标题栏）。
